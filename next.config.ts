/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'hips.hearstapps.com',
      'images.unsplash.com',
      'cdn.example.com',
    ],    
  },
};

module.exports = nextConfig;
